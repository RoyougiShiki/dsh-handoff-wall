# 交接板（dsh-handoff-board）

给 DSH 的会话间交接机制：换班时写下「交接条」，条按项目串成「线程」，人和 AI 都能翻。本文件只定义语言，不记录实现（实现见 `.scratch/v0/` 的地图与票）。

## Language

### 核心物件

**交接条（Note）**:
一段工作收尾时写下的六段状态索引，是接手者的事实契约。
_Avoid_: 摘要、总结、handoff 文件、笔记

**线程（Thread）**:
同一项目下按时间排列的交接条序列。
_Avoid_: 链、任务、分支

**交接板（Board）**:
全部线程与其背后的账本合在一起的整体；界面上呈现为一面按时间排队墙。
_Avoid_: 画布、数据库、看板

### 来源等级

**正式条（Curated Note）**:
经工人生成或人工确认的交接条，图上一等公民。
_Avoid_: 白节点、好节点

**占位卡（Raw Card）**:
没写过交接条的时段由程序机械补出的低置信索引卡（标题/时段/碰过的文件），仅保墙面连续。
_Avoid_: 灰节点、假节点、灰卡（口语可用，文档统一用占位卡）

**回填（Backfill）**:
生成占位卡的补漏动作。
_Avoid_: 补录、修复

### 动作与人

**换班（Handover）**:
一个阶段完成、工作移交给下一个会话的边界时刻——唯一需要人动手的时刻。
_Avoid_: 切换、迁移

**工人（Worker）**:
生成交接条的那次一次性独立模型调用，与会话自身的主模型健康无关。
_Avoid_: 小模型、子代理

**接续（Continue）**:
从一条交接条开出新会话并把六段全文注入为首条消息的动作。
_Avoid_: 恢复、复活、续命

**撞车检查（Who Else）**:
开工前查询「还有哪个活着的会话最近动过这个文件」。
_Avoid_: 冲突检测、锁
